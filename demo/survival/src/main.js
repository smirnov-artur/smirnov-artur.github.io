import*as THREE from"three";import{EffectComposer}from"three/addons/postprocessing/EffectComposer.js";import{RenderPass}from"three/addons/postprocessing/RenderPass.js";import{UnrealBloomPass}from"three/addons/postprocessing/UnrealBloomPass.js";import{createGame}from"./state.js";import*as world from"./world.js";import*as player from"./player.js";import*as camera from"./camera.js";import*as combat from"./combat.js";import*as enemies from"./enemies.js";import*as items from"./items.js";import*as loot from"./loot.js";import*as inventory from"./inventory.js";import*as ui from"./ui.js";import*as demo from"./demo.js";import*as icons from"./icons.js";import*as inventory_ui from"./inventory_ui.js";import*as allies from"./allies.js";import*as tavern from"./tavern.js";import*as recorder from"./recorder.js";const game=createGame();window.game=game;const canvas=document.getElementById("c");const renderer=new THREE.WebGLRenderer({canvas,antialias:false});renderer.setSize(window.innerWidth,window.innerHeight);renderer.setPixelRatio(1);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.BasicShadowMap;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.outputColorSpace=THREE.SRGBColorSpace;const scene=new THREE.Scene;scene.background=new THREE.Color(14262374);scene.fog=new THREE.FogExp2(14067833,.0032);const cam=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,500);cam.position.set(Math.sin(-Math.PI*.65)*44,29,Math.cos(-Math.PI*.65)*44);cam.lookAt(0,4.5,0);const composer=new EffectComposer(renderer);composer.addPass(new RenderPass(scene,cam));const bloomPass=new UnrealBloomPass(new THREE.Vector2(innerWidth,innerHeight),.85,.55,.72);composer.addPass(bloomPass);window.addEventListener("resize",()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);composer.setSize(innerWidth,innerHeight)});game.scene=scene;game.camera=cam;game.renderer=renderer;game.composer=composer;game.clock=new THREE.Clock;addEventListener("keydown",e=>{game.input.keys[e.code]=true});addEventListener("keyup",e=>{game.input.keys[e.code]=false});const modules=[world,player,camera,combat,enemies,items,loot,inventory,ui,demo,icons,inventory_ui,allies,tavern,recorder];for(const m of modules)m.init(game);let fpsAcc=0,fpsFrames=0,fpsTimer=0;function animate(){requestAnimationFrame(animate);const dt=Math.min(.1,game.clock.getDelta());game.dt=dt;game.time+=dt;for(const m of modules)m.update(game,dt);composer.render();fpsAcc+=dt;fpsFrames++;if(fpsAcc>=.5){game.fps=fpsFrames/fpsAcc;fpsAcc=0;fpsFrames=0}}animate();
